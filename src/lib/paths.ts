import path from "path";

export const ROOT = process.cwd();
export const STORY_DIR = path.join(ROOT, "story");
export const KB_DIR = path.join(ROOT, "kb");
export const WORK_DIR = path.join(ROOT, "work");
export const PROJECT_FILE = path.join(ROOT, "project.json");
